rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /salons/default/dates/{date}/appointments/{appointmentId} {
      allow read, write: if request.auth != null;
    }
  }
}
